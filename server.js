const express = require("express");
const Task = require("./db");
const app = express();

app.use(express.json());

// POST Request

// GET Request

// PUT Request

// Delete Request

app.listen(1337, () => {
  "server is up and running";
});
